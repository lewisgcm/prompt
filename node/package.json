{
  "name": "prompt",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --workspace=@prompt/models run build",
    "test": "npm --workspace=@prompt/models run test"
  },
  "workspaces": [
    "packages/*"
  ]
}